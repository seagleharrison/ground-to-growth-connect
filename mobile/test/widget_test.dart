// Placeholder kept so `flutter test` has a default entry point; the real
// UI tests live in profile_editing_test.dart and document_walkthrough_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
